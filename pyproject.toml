[project]
name = "textual-stopwatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "textual>=8.2.8",
]

[dependency-groups]
dev = [
    "textual-dev>=1.8.0",
    "watchfiles>=1.2.0",
]
